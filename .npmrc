# Pin every direct dependency to an exact version (no ^ or ~ ranges).
save-exact=true

# Always create/update a lockfile so the full dependency tree is reproducible.
package-lock=true

# Fail (non-zero) if a high/critical vulnerability is present in the tree.
audit-level=high

# Use the official public registry only.
registry=https://registry.npmjs.org/
